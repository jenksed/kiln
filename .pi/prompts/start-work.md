Start Kiln work package {{work_package}}.

Load and follow the `kiln-work-package` skill.

Do not edit source code until you have:

1. run `scripts/agent-preflight`;
2. read the matching plan;
3. listed applicable `KILN-INV-*` identifiers and architecture decision records;
4. inspected current source, tests, Git state, and dependency direction;
5. stated the expected mutation surface and narrow checks.

Return the orientation handoff from the skill. Stop if the branch, plan, or accepted requirements do not agree.
